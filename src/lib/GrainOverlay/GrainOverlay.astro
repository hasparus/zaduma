---
import grainSvg from "./grain.svg";

interface Props {
  /** number between 0 and 1 */
  opacity: number;
}
const { opacity } = Astro.props;
---

<div
  style={{ opacity, background: `url(${grainSvg})` }}
  class="pointer-events-none absolute inset-0 mix-blend-difference select-none"
>
</div>
