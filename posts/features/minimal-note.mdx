This post is a simple markdown file. Its title is derived from the filename. Its
date is computed from git history.

When you need more, [use frontmatter](./post-with-frontmatter).

---

```mdx
This post is a simple markdown file. Its title is derived from the filename. Its
date is computed from git history.

When you need more, [use frontmatter](./post-with-frontmatter).
```
